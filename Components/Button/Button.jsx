import React from 'react'
import styled from 'styled-components/native'

const Button = styled.Button`
width: auto;

  height: auto;

  padding: 12px;

  border-radius: 10px;
  
`;

const MyCompaButton = ({ title }) => (


    <Button>mm</Button>

);
export default MyCompaButton;